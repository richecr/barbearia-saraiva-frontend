import React from 'react';

/**
 * Componente que irá renderizar quando o usuário acessa uma rota não mapeada.
 *
 * @returns
 */
const NotFoundPage = () => {
  return <span>Página não encontrada :(</span>;
};

export default NotFoundPage;
