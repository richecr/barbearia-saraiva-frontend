import React from 'react';

/**
 * Componente que irá renderizar a página de usuário sem pemrissão.
 *
 * @returns
 */
const NotAuthorizedPage = () => {
  return <span>Você não tem autorização para acessar essa página!</span>;
};

export default NotAuthorizedPage;
